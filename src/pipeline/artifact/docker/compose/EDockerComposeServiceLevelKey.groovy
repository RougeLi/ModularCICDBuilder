package pipeline.artifact.docker.compose

enum EDockerComposeServiceLevelKey {
    container_name,
    build,
    image,
    command,
    ports,
    environment,
    env_file,
    configs,
    secrets,
    init,
    scale,
    volumes,
    networks,
    depends_on,
    restart,
    entrypoint,
    labels,
    expose,
    links,
    logging,
    security_opt,
    ulimits,
    volumes_from,
    cap_add,
    cap_drop,
    extra_hosts,
    external_links,
    deploy,
    healthcheck,
    stop_signal,
    stop_grace_period,
    sysctls,
    tmpfs,
    cgroup_parent,
    device_cgroup_rules,
    dns,
    dns_search,
    dns_option,
    privileged,
    user,
    working_dir
}
